You need to run "make yaml" to build the yaml libraries. Make sure you have cmake installed. 
You may have to edit the flags in build_yaml.sh, especially -DCMAKE_CXX_COMPILER=/opt/local/bin/g++
(This was needed on a mac, but probably not on linux.)
